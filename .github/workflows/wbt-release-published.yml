name: Build and Release Binaries
on:
  release:
    types: [published]

permissions: {}

env:
  CMD: wow-build-tools

jobs:
  move-major-tag:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      tag_name: ${{ steps.create-or-update-tag.outputs.tag_name }}
    steps:
      - name: Create or Update Major Version Tag
        id: create-or-update-tag
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        with:
          script: |
            const fullVersion = process.env.GITHUB_REF_NAME;
            const majorVersion = fullVersion.split('.')[0];
            const tagName = fullVersion.includes('-') ? `${majorVersion}-${fullVersion.split('-')[1].split('.')[0]}` : majorVersion;

            await github.rest.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: `refs/tags/${tagName}`,
              sha: context.sha
            }).catch(async (error) => {
              if (error.status === 422) {
                await github.rest.git.updateRef({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  ref: `tags/${tagName}`,
                  sha: context.sha,
                  force: true
                });
              } else {
                throw error;
              }
            });

            core.setOutput('tag_name', tagName);

  setup-go:
    # Do it once, leverage cache for subsequent jobs.
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache-dependency-path: |
            go.sum

      - name: Go cache paths
        id: go-cache-paths
        run: |
          [ -d $(go env GOCACHE) ] && chmod -R 0755 $(go env GOCACHE)
          [ -d $(go env GOMODCACHE) ] && chmod -R 0755 $(go env GOMODCACHE)
          echo "go-build=$(go env GOCACHE)" >> $GITHUB_OUTPUT
          echo "go-mod=$(go env GOMODCACHE)" >> $GITHUB_OUTPUT

      # Cache go build cache, used to speedup go test
      - name: Go Build Cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ steps.go-cache-paths.outputs.go-build }}
          key: ${{ runner.os }}-go-build-${{ hashFiles('**/go.sum') }}

      # Cache go mod cache, used to speedup builds
      - name: Go Mod Cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ steps.go-cache-paths.outputs.go-mod }}
          key: ${{ runner.os }}-go-mod-${{ hashFiles('**/go.sum') }}

      - name: Install Go dependencies
        run: go mod download

  build:
    runs-on: ubuntu-latest
    needs: setup-go
    strategy:
      matrix:
        goos: [linux, darwin, windows]
        goarch: [amd64, arm64]
    permissions:
      contents: write
      actions: read
    env:
      # Use the Git tag as the version (for example, "v1.2.3" or "v1.2.3-alpha.1")
      VERSION: ${{ github.ref_name }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache-dependency-path: |
            go.sum

      - name: Go cache paths
        id: go-cache-paths
        run: |
          [ -d $(go env GOCACHE) ] && chmod -R 0755 $(go env GOCACHE)
          [ -d $(go env GOMODCACHE) ] && chmod -R 0755 $(go env GOMODCACHE)
          echo "go-build=$(go env GOCACHE)" >> $GITHUB_OUTPUT
          echo "go-mod=$(go env GOMODCACHE)" >> $GITHUB_OUTPUT

      # Cache go build cache, used to speedup go test
      - name: Go Build Cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ steps.go-cache-paths.outputs.go-build }}
          key: ${{ runner.os }}-go-build-${{ hashFiles('**/go.sum') }}

      # Cache go mod cache, used to speedup builds
      - name: Go Mod Cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ steps.go-cache-paths.outputs.go-mod }}
          key: ${{ runner.os }}-go-mod-${{ hashFiles('**/go.sum') }}

      - name: Replace version token in source
        run: |
          echo "Replacing version constant with ${VERSION}"
          sed -i "s/const version = \"LOCAL\"/const version = \"${VERSION}\"/" ./internal/update/updatebin.go

      - name: Build binary
        run: |
          mkdir -p build
          ext=""
          if [ "${{ matrix.goos }}" = "windows" ]; then ext=".exe"; fi
          GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -o build/${CMD}_${{ matrix.goos }}_${{ matrix.goarch }}${ext} .

      - name: Zip binary
        run: |
          # Determine the file extension based on OS.
          ext=""
          if [ "${{ matrix.goos }}" = "windows" ]; then ext=".exe"; fi
          binaryFile="build/${{ env.CMD }}_${{ matrix.goos }}_${{ matrix.goarch }}${ext}"
          zipFile="build/${{ env.CMD }}_${{ matrix.goos }}_${{ matrix.goarch }}.zip"
          echo "Zipping ${binaryFile} to ${zipFile}"
          zip -j ${zipFile} ${binaryFile}

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: zip-${{ env.CMD }}_${{ matrix.goos }}_${{ matrix.goarch }}
          path: build/${{ env.CMD }}_${{ matrix.goos }}_${{ matrix.goarch }}.zip

  upload-assets:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
      actions: read
    outputs:
      release-type: ${{ steps.release-type.outputs.release-type }}
    steps:
      - name: Download artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: archived-binaries
          pattern: zip-*
          merge-multiple: true

      - name: Upload Release Asset
        uses: softprops/action-gh-release@efb35369e0ad2afab669f228072c1b0d510eae64 # v3.0.3
        with:
          tag_name: ${{ github.ref_name }}
          # The glob pattern matches the asset that follows your naming convention.
          files: archived-binaries/*.zip

      - name: Output release type
        id: release-type
        run: |
          if [[ ${{ github.ref_name }} == *alpha* ]]; then
            release_type="alpha"
          elif [[ ${{ github.ref_name }} == *beta* ]]; then
            release_type="beta"
          else
            release_type="release"
          fi
          echo "release-type=${release_type}" >> $GITHUB_OUTPUT

  selfupdate-smoke-test:
    # Downloads the previous release's binary and runs its `update` subcommand
    # against this release, to catch self-update regressions no unit test can
    # (mocked interface tests only re-run assertions already written; this
    # exercises the real download/decompress/replace path against the real
    # GitHub API). Runs in parallel with `distribute` -- neither depends on
    # the other -- so a failure here never blocks or delays actually shipping
    # this release to players.
    runs-on: ubuntu-latest
    needs: upload-assets
    timeout-minutes: 10
    permissions:
      contents: read
    env:
      GH_TOKEN: ${{ github.token }}
      REPO: McTalian-WoW-Addons/wow-build-tools
    steps:
      - name: Determine previous release
        id: previous
        run: |
          # The release that triggered this workflow is the newest one, so
          # take the next-newest published, non-prerelease tag.
          mapfile -t tags < <(gh release list --repo "$REPO" --exclude-drafts --exclude-pre-releases --limit 2 --json tagName --jq '.[].tagName')
          if [ "${#tags[@]}" -lt 2 ]; then
            echo "Fewer than two releases exist -- nothing to self-update from. Skipping."
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "skip=false" >> "$GITHUB_OUTPUT"
          echo "previous_tag=${tags[1]}" >> "$GITHUB_OUTPUT"

      - name: Download previous release binary
        if: steps.previous.outputs.skip == 'false'
        run: |
          mkdir -p ./smoke-test
          gh release download "${{ steps.previous.outputs.previous_tag }}" --repo "$REPO" \
            --pattern "${CMD}_linux_amd64.zip" --dir ./smoke-test
          unzip -j ./smoke-test/${CMD}_linux_amd64.zip -d ./smoke-test
          chmod +x ./smoke-test/${CMD}_linux_amd64

      - name: Verify self-update
        if: steps.previous.outputs.skip == 'false'
        run: |
          bin=./smoke-test/${CMD}_linux_amd64
          expected="${{ github.ref_name }}"

          # Releases published before the --version flag existed (see #216)
          # don't support it at all, so the downloaded previous-release binary
          # can legitimately fail this check once, right after --version was
          # added -- don't hard-fail the job for that, since it's expected and
          # self-resolving (every release from here on supports --version).
          # The assertion that actually matters is the post-update one below.
          before="$("$bin" --version 2>/dev/null | awk '{print $NF}')"
          if [ -z "${before}" ]; then
            echo "Previous release (${{ steps.previous.outputs.previous_tag }}) does not support --version, skipping before-check."
          elif [ "${before}" != "${{ steps.previous.outputs.previous_tag }}" ]; then
            echo "::error::downloaded binary reports version ${before}, expected ${{ steps.previous.outputs.previous_tag }}"
            exit 1
          else
            echo "Before: ${before}"
          fi

          # `update`'s Cobra Run func never returns an error (see cmd/update.go),
          # so it always exits 0 regardless of whether the self-update actually
          # succeeded -- verify by re-checking --version instead of trusting the
          # exit code. Release assets can also take a moment to become fully
          # queryable right after publish, so poll a few times before giving up
          # (same idea as the probe methodology used to validate the self-update
          # library swap in internal/update).
          after="${before}"
          attempt=0
          max_attempts=6
          while [ "${after}" != "${expected}" ] && [ "${attempt}" -lt "${max_attempts}" ]; do
            attempt=$((attempt + 1))
            echo "Running self-update (attempt ${attempt}/${max_attempts})..."
            "$bin" update || true
            after="$("$bin" --version | awk '{print $NF}')"
            if [ "${after}" != "${expected}" ]; then
              echo "Not yet updated (still ${after}), retrying in 20s..."
              sleep 20
            fi
          done

          if [ "${after}" != "${expected}" ]; then
            echo "::error::expected version ${expected} after self-update, got ${after} (gave up after ${max_attempts} attempts)"
            exit 1
          fi
          echo "Self-update verified: ${before} -> ${after}"

  distribute:
    runs-on: ubuntu-latest
    needs: upload-assets
    strategy:
      matrix:
        distro: [curse, wago, wowi]
    permissions:
      contents: read
      actions: read
    env:
      CURSE_ID: 1198075
      WAGO_ID: QKywW7N7
      WOWI_ID: 26905
      CF_API_KEY: ${{ secrets.CF_API_KEY }}
      WOWI_API_TOKEN: ${{ secrets.WOWI_API_TOKEN }}
      WAGO_API_TOKEN: ${{ secrets.WAGO_API_TOKEN }}
      COMMON_ARGS: "--interface-versions=110100 -i ./wow-build-tools-${{ github.ref_name }}.zip -l ${{ github.ref_name }} -r ${{ needs.upload-assets.outputs.release-type }} -V"
      VERSION: ${{ github.ref_name }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Download artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: zip-${{ env.CMD }}_windows_amd64
          path: bin

      - name: Create a windows zip for upload to distros
        run: |
          echo "Generating README.txt"
          rm -f ./bin/README.txt
          touch bin/README.txt
          echo "This is NOT a WoW addon, it is a tool for building and releasing WoW addons." >> bin/README.txt
          echo "It is a development tool. This particular zip is uploaded to the three popular WoW Addon distribution platforms - Hello folks at ${{ matrix.distro }}!" >> bin/README.txt
          echo "It contains the Windows AMD64 binary within ${CMD}_windows_amd64.zip." >> bin/README.txt
          echo "" >> bin/README.txt
          echo "For more information, see the GitHub repository which has releases for other platforms as well as a Wiki." >> bin/README.txt
          echo "https://github.com/McTalian-WoW-Addons/${{ env.CMD }}" >> bin/README.txt
          echo "" >> bin/README.txt
          echo "Please report any issues you find on GitHub." >> bin/README.txt
          echo "Thank you for reading this and for your interest in ${{ env.CMD }} :)" >> bin/README.txt
          echo "" >> bin/README.txt
          echo "- McTalian" >> bin/README.txt
          echo "" >> bin/README.txt
          echo "Copying LICENSE to bin"
          cp LICENSE ./bin/LICENSE
          echo "Generating ${{ env.CMD }}.toc (mainly for Wago)"
          rm -f ./bin/${{ env.CMD }}.toc
          touch bin/${{ env.CMD }}.toc
          echo "## Interface: 110100" >> bin/${{ env.CMD }}.toc
          echo "## Title: ${{ env.CMD }}" >> bin/${{ env.CMD }}.toc
          echo "## Notes: This is NOT an addon, but a tool for building and releasing WoW addons." >> bin/${{ env.CMD }}.toc
          echo "## Author: McTalian" >> bin/${{ env.CMD }}.toc
          echo "" >> bin/${{ env.CMD }}.toc
          echo "Putting it all in a zip (wrapped in ${{ env.CMD }}/ folder for CurseForge)"
          mkdir -p ./${{ env.CMD }}
          cp ./bin/${{ env.CMD }}_windows_amd64.zip ./bin/README.txt ./bin/LICENSE ./bin/${{ env.CMD }}.toc ./${{ env.CMD }}/
          cd . && zip -r ./${{ env.CMD }}-${{ env.VERSION }}.zip ./${{ env.CMD }}/

      - name: Download binary
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: zip-${{ env.CMD }}_linux_amd64

      - name: Extract the linux binary
        run: |
          mkdir -p ./bin
          unzip -j ${{ env.CMD }}_linux_amd64.zip -d ./bin

      - name: Upload to CurseForge
        if: ${{ matrix.distro == 'curse' }}
        run: |
          ./bin/${{ env.CMD }}_linux_amd64 upload curse -p ${{ env.CURSE_ID }} ${{ env.COMMON_ARGS }}

      - name: Upload to Wago
        if: ${{ matrix.distro == 'wago' }}
        run: |
          ./bin/${{ env.CMD }}_linux_amd64 upload wago -a ${{ env.WAGO_ID }} ${{ env.COMMON_ARGS }}

      - name: Upload to WoWInterface
        if: ${{ matrix.distro == 'wowi' }}
        run: |
          ./bin/${{ env.CMD }}_linux_amd64 upload wowi -w ${{ env.WOWI_ID }} ${{ env.COMMON_ARGS }} --project-version=${{ github.ref_name }}
